# Memory — Corsair

## Workflow Rules
- **Always restart Docker after finishing a task**: Run `docker-compose up --build -d` after completing any code change to rebuild and restart the container.
- **Always push to GitHub after finishing a task**: Run `git push` after committing changes.
